# AD-Alterome Report Version Backup

Backup created before adding `adalterome-case-study-expert`.

Source commit at backup time:

```text
7c82eea Use full-pool AD-Alterome curation for report-grade skills
```

Backed-up skill folders:

- `adalterome-api`
- `adalterome-report`
- `adalterome-gene-report`
- `adalterome-term-report`
- `adalterome-hypothesis-report`
- `adalterome-compare-report`

Project backup path:

```text
adalterome-skills/backups/report-version-2026-06-04/skills/
```

Installed-skill backup path:

```text
${CODEX_HOME:-$HOME/.codex}/skills/_backups/adalterome-report-version-2026-06-04/skills/
```

Restore by copying the desired backed-up skill folder back into the active skills directory.
